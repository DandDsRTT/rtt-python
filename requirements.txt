# Runtime + test dependencies, pinned to the versions the app is developed and
# tested against, so the Render build (see render.yaml / .python-version) matches local.
sympy==1.14.0
numpy==2.4.6
scipy==1.17.1

pytest==9.0.3
pytest-asyncio==1.3.0  # drives the in-process NiceGUI render tests (the User fixture is async)

# web UI (monolith front end)
nicegui==3.12.0
